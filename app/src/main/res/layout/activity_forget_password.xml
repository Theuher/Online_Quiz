<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center_vertical">

    <!-- Title -->
    <TextView
        android:id="@+id/forgot_password_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Нууц үг мартсан?"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_marginBottom="24dp"
        android:textColor="@color/black"/>

    <!-- Email Input -->
    <EditText
        android:id="@+id/email"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Е-Мейл хаяг"
        android:inputType="textEmailAddress"
        android:textSize="16sp"
        android:padding="12dp"
        android:background="@drawable/grey_edittext"
        android:textColor="@color/black"
        android:layout_marginBottom="16dp"/>

    <!-- Instructions or Text for the User -->
    <TextView
        android:id="@+id/instruction_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Та бүртгэлтэй е-майл хаягаа оруулна уу. Танд нууц үг сэргээх холбоос ирнэ."
        android:textSize="14sp"
        android:textColor="@color/black"
        android:layout_marginBottom="24dp"
        android:gravity="center"/>

    <!-- Reset Password Button -->
    <Button
        android:id="@+id/reset_password_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Нууц үг сэргээх"
        android:textSize="16sp"
        android:textColor="@android:color/white"
        android:background="@color/purple_700"
        android:layout_marginBottom="16dp"
        android:textAllCaps="false"/>

    <!-- Back to Login Link -->
    <TextView
        android:id="@+id/back_to_login"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Нэвтрэх хуудас руу буцах"
        android:textSize="14sp"
        android:textColor="@color/purple_700"
        android:gravity="center"
        android:layout_marginTop="8dp"/>

</LinearLayout>
